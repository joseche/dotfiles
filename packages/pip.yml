# vim:ft=ansible:
---
- name: include server pip modules
  include: pip-servers.yml
- name: pip modules
  pip: name={{item}} state=present
  become: true
  become_user: root
  with_items:
    - pdb
    - Django
    - mccabe
    - pylama
    - pycodestyle
