# vim:ft=ansible:
---
- name: pip modules for servers
  pip: name={{item}} state=present
  become: true
  become_user: root
  with_items:
    - powerline-status
    - neovim
    - pep8-naming
    - flask
    - nose
    - nose-cov
    - rednose
    - pytest
    - pytest-cov
    - mock
    - requests
