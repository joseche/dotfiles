# vim:ft=ansible:
---
- name: "Add base packages for debian"
  pkg: name={{item}} state=present
  become: true
  become_user: root
  with_items:
    - python3-dev
    - python3-pudb
    - golang
    - ansible
    - fonts-powerline
